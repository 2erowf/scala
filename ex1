//In the Scala REPL, compute the square root of 3, and then square that value.
//By how much does the result differ from 3? (Hint: The res variables are your
//friend.)
import scala.math._
sqrt(3)
sqrt(res1)

//Scala lets you multiply a string with a number—try out "crazy" * 3 in the REPL.
//What does this operation do? Where can you find it in Scaladoc?
"crazy" * 3 //crazycrazycrazy
"crazy".*(3) //longer form

//What does 10 max 2 mean? In which class is the max method defined?
// similar max(a,b) in java
//defined in RichInt
10 max 2
10.max(2)

//BigInt can be constructed using int,Using BigInt, compute 21024
BigInt(2) pow 1024
BigInt(2)

//What do you need to import so that you can get a random prime as
//probablePrime(100, Random), without any qualifiers before probablePrime and Random?

//import the exact method name to use the method without any pre qualifiers
import BigInt.probablePrime
import util.Random
probablePrime(100,Random)

//One way to create random file or directory names is to produce a random
//BigInt and convert it to base 36, yielding a string such as "qsnvbevtomcj38o06kul".
//Poke around Scaladoc to find a way of doing this in Scala.
probablePrime(100,Random) toString 36

//How do you get the first character of a string in Scala? The last character?
val test = "fannnnn"
test(test.size - 1)
test(0)
